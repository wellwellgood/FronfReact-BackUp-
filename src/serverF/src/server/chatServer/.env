DB_HOST=chat-db.abcd1234.cloudtype.app   # ← 여기에 실제 DB 호스트 주소 입력
DB_PORT=3306
DB_USER=master                           # ← DB 생성 시 입력한 사용자명
DB_PASSWORD=0213                         # ← DB 생성 시 입력한 비밀번호
DB_NAME=chat                             # ← MariaDB에 만든 DB 이름

# 서버 포트 (Render는 보통 PORT 환경변수로 자동 할당함)
PORT=3000
